using UnityEngine;
using System.Collections;

public class InfoHit : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	/*
	void OnTriggerStay2D(Collider2D other) {


		if (other.gameObject.tag == "Info")
		{
			transform.localPosition = new Vector2(-2.5f, 0.5f);
			
		}

		if (other.gameObject.tag == "InfoTwo")
		{
			transform.localPosition = new Vector2(-2.5f, -1f);
			
		}
		
	}


	void OnTriggerExit2D(Collider2D other) {
		
		if (other.gameObject.tag == "Info")
		{
			transform.localPosition = new Vector2(-2.5f, 2f);
			
		}
		
		if (other.gameObject.tag == "InfoTwo")
		{
			transform.localPosition = new Vector2(-2.5f, 2f);
			
		}
		
	}
	*/
}
